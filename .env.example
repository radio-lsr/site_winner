# ============================================================
# Variables pour docker-compose.yml (racine du projet)
# Copiez vers `.env` puis personnalisez :
#   cp .env.example .env
# ============================================================

# Mot de passe root MySQL (conteneur db)
MYSQL_ROOT_PASSWORD=winner_root_2026

# Secret JWT — générez-en un fort :
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=9f2c8e1a7b4d4f6e8a0c2d4b6e8f0a1c3d5e7f9a1b3c5d7e9f0a2b4c6d8e0f1a

# Compte administrateur créé au premier démarrage
ADMIN_EMAIL=admin@winner.local
ADMIN_PASSWORD=Admin@Winner2026

# URL publique du site (utilisée dans les emails de réinitialisation)
FRONTEND_URL=http://localhost:8080
